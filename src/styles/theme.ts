import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    snowy: {
      main: "#fff",
    },
    grayLight: {
      main: "#F6F6F6",
    },
    grayDark: {
      main: "#696969",
      light: "#d9d9d9",
    },
  },
});

export default theme;
